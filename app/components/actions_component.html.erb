<%= link_to t('edit'), edit_path(subject), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
<%= link_to t('delete'), delete_path(subject), method: :delete, data: {confirm: "Are you sure?"} %>
